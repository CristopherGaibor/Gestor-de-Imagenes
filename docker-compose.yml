version: '3.8'

services:
  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    environment:
      - DB_PATH=/app/data/auth.db
    volumes:
      - auth_data:/app/data

  image-service:
    build:
      context: ./image-service
      dockerfile: Dockerfile
    ports:
      - "8082:8080"
    volumes:
      - image_data:/app/data

volumes:
  auth_data:
  image_data:
